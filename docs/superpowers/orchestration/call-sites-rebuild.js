export const meta = {
  name: 'call-sites-rebuild',
  description: 'Rebuild Call sites against the Repository Index Explorer reference',
  phases: [
    { title: 'Rebuild' },
    { title: 'Verify' },
  ],
}

const ROOT = 'C:/Users/sebastianr/Desktop/Terminal/Claude/Sync'
const REF = 'docs/stitch_sync_developer_console/stitch_sync_developer_console/repository_index_explorer'

const BASE = [
  'You are working in ' + ROOT + ' on the Sync operator console.',
  '',
  'READ FIRST:',
  '- docs/superpowers/plans/2026-08-26-ui-rebuild-master-brief.md',
  '- web/CLAUDE.md -- what a screen may and may not claim. This OUTRANKS the reference.',
  '- .claude/rules/console-surface.md and DESIGN.md -- the token contract.',
  '',
  'THE SCREEN: web/src/features/bindings/call-sites-page.tsx (616 lines). The route is',
  '/repositories/:repoId/call-sites.',
  '',
  'NOTE ITS NEIGHBOUR: features/bindings/binding-surface-page.tsx in the SAME directory was rebuilt',
  'on 2026-08-26 (CI-W650) against THIS SAME reference -- four locked panes, nine columns down to',
  'five, 346 words down to 190. Read it first. It is the detail view over one operation and yours is',
  'the directory over all of them, so they should read as one family without being one screen. It',
  'also shares `call-site-columns.ts` and `filter-rail.tsx` with you -- both were edited by that',
  'rebuild, so read their CURRENT state rather than assuming.',
  'LIVE URL: http://localhost:5173/repositories/synthetic%2Fevery-state/call-sites',
  '',
  '**THE REFERENCE IS CHOSEN AND IT IS NOT NEGOTIABLE: ' + REF + '**',
  'The owner named this screen specifically. Open ' + REF + '/screen.png and study it, then read',
  REF + '/code.html for the composition behind it. Build THIS.',
  '',
  'THE OWNER SAID, VERBATIM: "As for the call sites page, there\'s not a lot of information and',
  'quality and design that\'s going on. So let\'s adopt this page style that is from the stitch',
  'front end designs."',
  '',
  'MEASURED IN CHROME 2026-08-26 -- what "not a lot of information" means concretely:',
  '- The screen IS already layout="locked", so this is NOT a layout fix. It is a DENSITY and',
  '  QUALITY upgrade. Do not congratulate yourself for the lock; it is already there.',
  '- ONE pane. ONE table. SIX columns. Fifty rows. ZERO charts. ZERO KPI tiles.',
  '- **"Narrow the call sites" appears TWICE in the source** (confirmed still true at 50796ac4).',
  '  That is a live defect; find it and fix it.',
  '- `MetricPanel` still wraps the table at :459-598 with a `text-figure` headline -- the audit',
  '  named this as one of four items on this screen's own spec list that were never carried out.',
  '  Read that spec (docs/superpowers/specs/2026-08-25-stitch-rebuild-specs.md) and do all four.',
  '- 718 words on screen for one table.',
  '',
  'WHAT THE DATA CAN ACTUALLY SUPPORT -- check the real payload before designing:',
  'The synthetic corpus under "synthetic/every-state" carries 56 call sites across 30 vendors and',
  '12 protocol kinds, all four binding rungs, and 210 vendor changes. Query the live API on 8787',
  'and look at a real payload before choosing any form. A panel that cannot draw its own data is',
  'the defect this console has already shipped twice.',
  '',
  'THE HONESTY RULES OUTRANK THE REFERENCE:',
  '- No composite score, health figure, traffic light or liveness pulse. If the reference carries',
  '  one, REFUSE IT and report the refusal. A previous lane correctly refused a pulsing "Tailing..."',
  '  dot on exactly this ground. A badge from a closed vocabulary, legible without its colour, is',
  '  permitted.',
  '- Absence is not zero. Staleness is not liveness. Never-measured is not nothing-here. Every',
  '  nothing gets its own sentence saying WHICH nothing it is.',
  '- No figure without its scope on screen, and no percentage without its denominator.',
  '- The provenance rung stays monochrome and is NEVER a hideable column.',
  '- The claim stays visible in the fewest honest words; only the ARGUMENT moves behind an info hint.',
  '',
  'CHART LAW (web/CLAUDE.md) -- this screen currently has no chart and may gain one:',
  '- Bars for rankings and for any set with meaningful zeros. A bar of length zero still has a row,',
  '  a label and a count.',
  '- Donuts ONLY where the parts sum to a whole a reader can name, and never below two members. A',
  '  donut cannot draw a zero -- one shipped over a set where four of five members were measured',
  '  zeros, rendered as a closed ring, and read as broken.',
  '- Log scale where the set spans orders of magnitude, and SAY SO on the chart.',
  '- ECharts owns anything with an axis, a legend or a time dimension. ranked-bars.tsx is SVG',
  '  because a bar row is a labelled rectangle whose width is a ratio.',
  '',
  'DISCIPLINE:',
  '- **Test first, and watch each new guard fail for the reason you expect** before trusting it.',
  '  Break the subject deliberately, watch the named test go red, restore. Console tests are',
  '  classification, derivation and structural invariants -- NEVER class names, NEVER snapshots.',
  '- Tailwind v4 trap: h-* derives from --spacing-*, so `h-row-lg` generates NOTHING. Use',
  '  `h-[var(--row-lg)]`. This rendered a pane header at 29px once.',
  '- Cascade layers: Tailwind utilities outrank @layer components regardless of specificity.',
  '- An IMPLICIT grid row is sized by its content BEFORE the `fr` rows divide what is left. Adding a',
  '  row to a two-track `.bento-lock` collapsed two panes to 131px and 87px. Declare the track.',
  '  `.bento-lock-3` exists in index.css if you need three.',
  '- No new token. A colour, type step, spacing value or radius is argued in DESIGN.md, never added',
  '  at a call site. No `bg-x/10` or `text-y/70` spelled inline.',
  '- The docked drawer is components/detail-layout.tsx -- a right-hand Sheet that never squeezes the',
  '  table. The owner ruled that shape for a call site explicitly: "the call site should be a drawer',
  '  view and not compact the screen."',
  '- If you factor shared code out of a file OUTSIDE this screen\'s directory, say so FIRST and',
  '  LOUDEST in forCoordinator.',
  '',
  'PATTERNS THAT LANDED AND WERE ACCEPTED -- read before writing:',
  '- web/src/features/vendors/repository-vendors-page.tsx (cards deck + docked drawer, zero tables)',
  '- web/src/features/findings/findings-page.tsx (locked table + drawer that never squeezes it)',
  '- web/src/components/pane.tsx (PanelPane: banded header, one scrolling body, pinned footer)',
  '',
  'GATE BEFORE REPORTING: `npx tsc -b` and `npx vitest run --maxWorkers=4` from web/. Read the',
  'FAILED list, never the count.',
  '',
  'VERIFY ON THE LIVE CONSOLE at 1920x1080 AND 1366x768. **Screenshots do not work in this session**',
  '-- the Browser pane does not composite frames, so every screenshot call times out. MEASURE THE',
  'DOM: computed overflow on main, document scrollHeight vs clientHeight, pane geometry, how many',
  'regions actually scroll, and that nothing is clipped inside its own box. Report the numbers.',
].join('\n')

phase('Rebuild')

const build = await agent([
  BASE,
  '',
  'YOUR TASK: rebuild Call sites against the Repository Index Explorer reference.',
  '',
  'Requirements, each checkable:',
  '1. Take the reference\'s COMPOSITION -- how it divides the surface, its density, the shape of its',
  '   panes -- not just its colours. The screen is already locked; what it lacks is structure and',
  '   information.',
  '2. Fix the duplicated "Narrow the call sites" heading.',
  '3. Justify every column that survives in the table. Move what a reader does not need per-row into',
  '   the docked drawer.',
  '4. Read the CURRENT page first and inventory every honest distinction it draws -- every place it',
  '   says WHICH nothing something is. The rebuild keeps all of them. Cutting prose is welcome;',
  '   losing a distinction is not.',
  '5. Anything you add must be answerable from the real payload. Query the API first.',
].join('\n'), {
  label: 'rebuild:call-sites',
  phase: 'Rebuild',
  // Isolated because the first run of this script was NOT, and it wrote straight into the main
  // tree beside another unisolated lane editing the same directory. It survived on luck.
  isolation: 'worktree',
  schema: {
    type: 'object',
    additionalProperties: false,
    required: ['done', 'isLocked', 'tscClean', 'vitestClean', 'files', 'deleted', 'measurements', 'forCoordinator'],
    properties: {
      done: { type: 'string', description: 'what the screen is now, top to bottom' },
      compositionTaken: { type: 'string', description: 'what was taken from the reference, concretely' },
      isLocked: { type: 'boolean' },
      duplicateHeadingFixed: { type: 'boolean' },
      tscClean: { type: 'boolean' },
      vitestClean: { type: 'boolean' },
      files: { type: 'array', items: { type: 'string' } },
      testsChanged: { type: 'array', items: { type: 'string' } },
      deleted: { type: 'array', items: { type: 'string' } },
      measurements: { type: 'string', description: 'live DOM numbers at BOTH resolutions' },
      refusals: { type: 'array', items: { type: 'string' } },
      blocked: { type: 'array', items: { type: 'string' } },
      forCoordinator: { type: 'string' },
    },
  },
})

phase('Verify')

const verdict = await agent([
  BASE,
  '',
  'A rebuild of Call sites claims to be finished. Try to REFUTE it. Default to refuted=true if you',
  'cannot confirm.',
  '',
  'THEIR CLAIM: ' + (build ? build.done : ''),
  'WHAT THEY TOOK FROM THE REFERENCE: ' + (build ? build.compositionTaken : ''),
  'THEIR MEASUREMENTS: ' + (build ? build.measurements : ''),
  '',
  'Check, in the tree and on the live console:',
  '1. **Did the information density actually increase?** Count panes, columns, distinct facts and',
  '   charts BEFORE (git HEAD) and AFTER. The owner\'s complaint was that there is not much',
  '   information or design here. A rebuild that renames things without adding legible information',
  '   has not answered it. Give both counts.',
  '2. Is the duplicated "Narrow the call sites" heading actually gone? Check the rendered DOM, not',
  '   the source.',
  '3. Did any honest distinction get lost? Run `git diff HEAD -- web/src/features/call-sites/` and',
  '   list any sentence that stated WHICH nothing something was and no longer does.',
  '4. Does anything now assert a claim the payload cannot support -- a score, a health figure, a',
  '   pulse, a percentage without its denominator, a figure with no scope? Does any chart draw a',
  '   form its own data cannot fill?',
  '5. Are the new tests real? Pick the two most load-bearing new guards, break the subject',
  '   deliberately, confirm each goes red for the stated reason, and restore.',
  '6. Run `npx tsc -b` and `npx vitest run --maxWorkers=4` yourself. Do not trust their report.',
].join('\n'), {
  label: 'refute:call-sites',
  phase: 'Verify',
  schema: {
    type: 'object',
    additionalProperties: false,
    required: ['refuted', 'findings'],
    properties: {
      refuted: { type: 'boolean' },
      densityBefore: { type: 'string' },
      densityAfter: { type: 'string' },
      findings: { type: 'array', items: { type: 'string' } },
      gatesPassed: { type: 'boolean' },
    },
  },
})

return { build, verdict }
