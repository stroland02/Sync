"""The case the scoping rule cannot know about, carrying its opt-out and its reason."""


def kept_for_the_sdk() -> None:  # lint-dead-links: allow - shipped in the 0.1 plugin SDK, external callers only
    return None
