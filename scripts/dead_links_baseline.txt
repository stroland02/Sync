# Public symbols nothing in `src/` reaches, accepted as of 2026-07-28.
#
# This is a list and never a threshold. A count absorbs both a repair and a new defect in
# silence; a named entry has to be deleted by whoever wires the symbol up, in the commit that
# wires it, because `lint_dead_links.py` fails on an entry that no longer describes anything.
# The file only shrinks. Adding a line is a decision, not a formality: it says a component is
# finished, tested, and reachable from nothing, and that somebody accepted that on purpose.
#
# Every entry below was already known and recorded in a spec before this lint existed. That is
# the evidence the lint works: it independently rediscovers what four separate manual sweeps
# found, and it is silent on `GraphStore.set_merge_outcome` and `sync.route.matrix.route()`,
# which were on the same list until they were wired.
#
# Format: <path from the repository root>:<qualname>

# `omit_property_at` stays because it is not the primitive `property_omit` needs: it counts
# lines from one where `omit_argument_at` counts from zero, it answers two ways where the
# remediator needs three -- "already correct" and "cannot establish" have opposite
# consequences -- and it has no spread guard. Wiring it would be an off-by-one and a
# collapsed distinction, which is worse than a baselined line.
src/sync/route/templates.py:omit_property_at

# The four below are a deferral and not an acceptance, which makes them a different kind of
# entry from the one above. `ea8f869` landed the M4 dashboard view models with their caller
# still unwritten: `src/sync/dashboard/__init__.py` says in as many words that Task 2 adds
# `build_app`, and `docs/superpowers/plans/2026-07-30-sync-m4-dashboard.md` has that task
# consuming exactly these functions. So the lint is right -- nothing in `src/` reaches them --
# and it is right about work in flight rather than about a decision anyone made.
#
# Whoever lands `build_app` deletes this block in the same commit. Leaving it would fail the
# lint on an entry that no longer describes a violation, which is the mechanism that stops a
# deferral from quietly becoming permanent.
src/sync/dashboard/queries.py:repository_overview
src/sync/dashboard/queries.py:vendor_detail
src/sync/dashboard/queries.py:finding_detail
src/sync/dashboard/queries.py:workflow_state

# `observed_error_windows` is a deferral like the block above, but a weaker one, and the
# difference is worth stating rather than borrowing the stronger framing. The dashboard entries
# name a written task that consumes them. This one does not: B71 landed the Sentry error-rate
# ingest deliberately without a detector, because a detector written against the single fixture
# that exists would be a detector tuned to that fixture. So the consumer is intended but
# unplanned, and no document names the commit that deletes this line.
#
# The reader stays rather than the tests reading SQL directly. Every assertion about what the
# ingest wrote goes through this method; replacing it with a query in the test file would put a
# second copy of the row mapping there, and a test that builds its own expected row out of the
# same SELECT it is checking is the shape of test this repository has twice found cannot fail.
#
# What retires this line is the first M5 detector querying these counts. Until then the entry is
# the honest record: the write path is wired and the read path is not.
src/sync/graph/store.py:GraphStore.observed_error_windows
